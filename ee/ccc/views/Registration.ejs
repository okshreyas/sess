<!DOCTYPE html>
<html>

<head>
    <title>Registration Form</title>
    <style>
        body {
            background-image: url('pix1.png');
            background-size: cover;
            background-repeat: no-repeat;
            background-attachment: fixed;
            background-position: center;
            image-rendering: pixelated;
            background-color: #b4b4b4;
            font-family: Arial, sans-serif;
            background-color: #f2f2f2;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .container {
            background-color: #ffffff;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            padding: 20px;
            width: 1200px;
            height: 600px;
            text-align: center;
            animation: fadeIn 0.2s ease-in-out;
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            grid-gap: 10px;
        }

        @keyframes fadeIn {
            0% {
                opacity: 0;
                transform: translateY(-10px);
            }
            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }

        h2 {
            color: #333;
            grid-column: span 3;
        }

        form {
            grid-column: span 3;
            display: grid;
            grid-template-columns: 2fr 2fr 2fr;
            grid-gap: 10px;
        }

        label {
            display: block;
            margin: 10px 0;
            color: #444;
            font-weight: bold;
            text-align: left;
            font-size: 14px;
        }

        input[type="text"],
        input[type="password"],
        input[type="email"],
        input[type="tel"] {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            width: 200px;
            padding: 12px;
            margin: 5px 0;
            border: none;
            border-radius: 10px;
            background-color: #ebedfd;
            color: #000000;
            outline: none;
            border-bottom: 0px solid #000000;
            transition: border-bottom 0.3s;
            position: relative;
            text-align: left;
        }

        input[type="text"]:focus,
        input[type="password"]:focus,
        input[type="email"]:focus,
        input[type="tel"]:focus {
            border-bottom: 2px solid #0a005f;
            transition: border-bottom 0s;
        }

        /* Custom Class for Line Animation */
        .line-animation {
            position: relative;
        }

        .line-animation::before {
            content: "";
            position: absolute;
            left: 0;
            bottom: -2px;
            width: 100%;
            height: 2px;
            background-color: #2f015a;
            transform: scaleX(0);
            transform-origin: right;
            transition: transform 0.3s;
        }

        .line-animation:focus::before {
            transform: scaleX(1);
            transform-origin: left;
        }

        select {
            width: 100%;
            padding: 12px;
            margin: 5px 0;
            border: none;
            border-radius: 0;
            background-color: transparent;
            color: #333;
            outline: none;
            border-bottom: 2px solid transparent;
            transition: border-bottom 3s;
        }

        select:focus {
            border-bottom: 2px solid #4CAF50;
        }

        input[type="submit"] {
            background-color: #3f0fc4;
            color: white;
            border: none;
            border-radius: 15px;
            cursor: pointer;
            margin: 0 auto;
            display: block;
            width: 200px;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            font-size: 20px;
        }

        input[type="submit"]:hover {
            width: 195px;
            transition: ease-in-out 0.3s;
            background-color: #000000;
        }

        .error {
    color: rgb(68, 0, 255);
    text-align: left;
    word-wrap: break-word; /* This rule ensures that long error messages wrap to the next line. */
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    font-weight: 700;
    font-size: 20px;
            border-radius: 10px;
            background-color: #ffffff;
            padding: 10px;
}


        .error-container {
            display: block;
            margin-top: 5px;
            text-align: left;
        }

        .login {
            background-color: #f0f0f0;
            color: white;
            font-size: 15px;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: #0306c9;
            border: 1px solid #100235;
            border-radius: 10px;
            padding: 10px;
            text-align: center;
        }

        /* .login:hover {
            width: 310px;
            transition: ease-in-out 0.3s;
            color: #000000;
        } */
    </style>
</head>

<body>
    <div class="container">
        <h2 style="color: #000000;">REGISTRATION</h2>
        <form action="/registration" method="post">
            <div>
                <label for="firstname">First Name</label>
                <input type="text" id="firstname" name="firstname" placeholder="Enter your first name" required>
            </div>
            <div>
                <label for="lastname">Last Name</label>
                <input type="text" id="lastname" name="lastname" placeholder="Enter your last name" required>
            </div>
            <div>
                <label for="empid">Employee ID</label>
                <input type="text" id="empid" name="empid" placeholder="Enter your employee ID" required>
            </div>
            <div>
                <label for="pass">Password</label>
                <input type="password" id="pass" name="pass" placeholder="Enter your password" required>
            </div>
            <div>
                <label for="confirmpass">Confirm Password</label>
                <input type="password" id="confirmpass" name="confirmpass" placeholder="Confirm your password" required>
                <div class="error-container">
                    <span id="passMatchError" class="error" style="color: red;"></span>
                </div>
            </div>
            <div>
                <label for="email">Email</label>
                <input type="email" id="email" name="email" placeholder="Enter your email" required>
            </div>
            <div>
                <label for="contactNumber">Contact Number:</label>
                <input type="tel" id="contactNumber" name="contactNumber" placeholder="Enter your contact number" required
                    pattern="[0-9]{10,12}" title="Please enter a numeric contact number with 10 digits">
            </div>
            <div>
                <label for="leademail">Lead Name</label>
                <input type="text" id="leademail" name="leademail" placeholder="Enter the lead name" required>
            </div>
            <div>
                <label for="manageremail">Manager Name</label>
                <input type="text" id="manageremail" name="manageremail" placeholder="Enter the manager name" required>
            </div>
            <input type="submit" value="Register" class="submit">
            <div class="login">
                <a href="/login"><b>Already have an account? Login</b></a>
            </div>
        </form>
    </div>

    <!-- Display the error message here -->
    <% if (typeof error !== 'undefined') { %>
    <p class="error"><%= error %></p>
    <% } %>

    <!-- Add the JavaScript validation script here -->
    <script>
        document.querySelector('form').addEventListener('submit', function (e) {
            const password = document.getElementById('pass').value;
            const confirmPassword = document.getElementById('confirmpass').value;
            if (password !== confirmPassword) {
                e.preventDefault();
                document.getElementById('passMatchError').textContent = 'Passwords do not match.';
            }
        });
    </script>
</body>

</html>
